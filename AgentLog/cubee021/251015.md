# TTTK 251015

# Start Log
- 20251015 10:30
- 작업자 : cubee021

## 현재 상황
- HouseGameMode, HouseGameState, HousePlayerState 헤더 파일 구조 완성
- HousePlayerState.cpp 구현 완료 (Replication, SetMissionComplete, ResetForNextMission)
- HouseGameState.cpp 구현 완료 (Replication, AreAllPlayersMissionComplete, GetFormattedTimer)
- **HouseGameMode.cpp 구현 필요** (거의 비어있음)

## To Do

1. **HouseGameMode.cpp 구현**
   - Constructor: GameState, PlayerState 클래스 설정
   - BeginPlay: 초기화 로직
   - Tick: 타이머 업데이트 로직
   - StartGame: 게임 시작 함수
   - UpdateMissionTimer: 미션 타이머 갱신
   - ReportMissionComplete: 플레이어 미션 완료 보고
   - StartMission: 특정 미션 시작
   - CheckMissionProgress: 미션 진행 체크
   - AdvanceToNextMission: 다음 미션으로 전환
   - FailMission: 미션 실패 처리

2. **코드 검증 및 컴파일 테스트**
   - 빌드 오류 체크
   - Replication 동작 검증
   - 멀티플레이 로직 검토

3. **Blueprint 연동 확인**
   - BP_HouseGameMode에서 설정 확인

---

## Compact Log 1
- 20251015 14:58

### 요약 내용

**HouseGameMode.cpp 전체 구현 완료**

1. **Constructor & BeginPlay**:
   - GameStateClass, PlayerStateClass 설정
   - Tick 비활성화 (Timer 기반으로 변경)
   - 초기 Phase를 WaitingToStart로 설정

2. **Timer 기반 시스템 구현**:
   - `FTimerHandle` 사용으로 Tick 대신 1초마다 타이머 업데이트
   - `UpdateMissionTimer()`: 1초마다 호출, 시간 감소 및 실패 체크
   - 30초/10초 경고 로그 출력

3. **게임 진행 로직**:
   - `StartGame()`: Cinematic Phase → 첫 번째 미션 시작, 타이머 시작
   - `StartMission()`: 미션 인덱스 설정, 모든 플레이어 상태 리셋
   - `ReportMissionComplete()`: 플레이어별 미션 완료 보고
   - `CheckMissionProgress()`: 모든 플레이어 완료 시 다음 미션으로

4. **미션 완료/실패 처리**:
   - `AdvanceToNextMission()`: 다음 미션 시작 또는 Victory 처리
   - `FailMission()`: GameOver Phase로 전환, 타이머 중지

5. **빌드 성공**:
   - 컴파일 에러 수정 (특수문자 제거)
   - 모든 기능 정상 빌드 완료

### Commit 정보

**Summary**: Implement HouseGameMode with timer-based mission system

1. **HouseGameMode 전체 구현**
   - Constructor: GameState/PlayerState 클래스 설정, Tick 비활성화
   - BeginPlay: 초기 상태 설정

2. **Timer 기반 미션 시스템**
   - FTimerHandle을 사용한 효율적인 타이머 관리
   - 1초마다 시간 감소 및 경고 로그 출력

3. **게임 흐름 로직**
   - StartGame: 게임 시작 및 첫 미션 진입
   - Mission 진행/완료/실패 처리 로직
   - Phase 전환 시스템 (WaitingToStart → Cinematic → Mission → Victory/GameOver)

4. **멀티플레이 지원**
   - 모든 플레이어 미션 완료 체크
   - PlayerState 기반 개별 진행 상황 관리
