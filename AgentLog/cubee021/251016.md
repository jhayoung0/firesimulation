# TTTK 251016

# Start Log
- 20251016 22:35
- 작업자 : cubee021

### To Do

1. 팀원 작업 분석 및 통합 이슈 해결
   - main 브랜치에서 가져온 변경사항 확인
   - PeopleOnePC 컨트롤러 상속 구조 이해
   - InGameUI 표시 문제 해결

2. MissionTable DataTable 시스템 구축
   - InGameWidget에 DataTable 연동
   - SetMissionTextFromIndex() 구현
   - 역할별 미션 텍스트 표시 시스템 설계

3. 멀티플레이어 역할 시스템 설계 논의
   - PlayerController 상속 구조 확인
   - 역할별 컨트롤러 사용 가능성 검토

---

## Compact Log 1
- 20251016 22:35

### 요약 내용

**1. 팀원 코드 통합 및 PlayerController 구조 분석**
- main 브랜치로부터 merge된 변경사항 확인 (commit: 8efc2bc)
- 팀원이 작성한 `PeopleOnePC`가 `firepjt_firstPlayerController`를 상속받은 구조 파악
- InGameWidget이 표시되지 않는 문제 원인 분석: GamePhase 이벤트 기반 위젯 생성 시스템 이해

**2. DataTable 기반 미션 시스템 구현**
- `InGameWidget.h`에 `MissionDataTable` 참조 변수 추가 (37-38번 줄)
- `SetMissionTextFromIndex()` 함수 구현:
  - DataTable에서 Row 검색 로직 구현 (FindRow<FMissionTable> 사용)
  - mission_head, mission_description을 UI TextBlock에 바인딩
  - Txt_Id 텍스트 포맷팅 버그 수정 (FString::Printf와 FText::FromString 사용)
- `firepjt_firstPlayerController`에서 InGameWidget 생성 시 MissionDataTable 할당 로직 추가

**3. 멀티플레이어 역할 시스템 설계 논의**
- 하나의 GameMode에서 여러 PlayerController 사용 가능 확인
- 역할별 컨트롤러 구현 방법 3가지 제시:
  1. PlayerController별 DataTable 설정 (간단)
  2. PlayerState 기반 역할 관리 (멀티플레이 권장)
  3. GameMode PostLogin 오버라이드 (중앙화)
- 테스트를 위해 우선 단일 DataTable(DT_People) 연결로 진행

### Commit 정보

"InGameWidget에 DataTable 기반 미션 시스템 구현"

1. "InGameWidget DataTable 연동"
   - InGameWidget.h: MissionDataTable 참조 변수 추가
   - SetMissionTextFromIndex(): DataTable Row 검색 및 UI 갱신 로직 구현
   - Txt_Id 텍스트 포맷팅 버그 수정 (FString::Printf 사용)

2. "PlayerController에서 MissionDataTable 할당"
   - firepjt_firstPlayerController.h: MissionDataTable UPROPERTY 추가
   - OnGamePhaseChanged(): InGameWidget 생성 시 DataTable 할당

3. "MissionTable include 추가"
   - InGameWidget.cpp: MissionTable.h 헤더 추가

---

## End Log
- 20251016 22:49
- 작업자 : cubee021

### 오늘 한 일

1. **팀원 코드 분석 및 통합 이슈 해결**
   - main 브랜치 merge 내역 분석 (맵 배치, 시네마틱, 미션 UI 데이터 테이블 등)
   - PeopleOnePC 컨트롤러 상속 구조 파악 및 InGameWidget 표시 문제 해결
   - HouseGameMode의 GamePhase 이벤트 기반 위젯 생성 시스템 이해

2. **DataTable 기반 미션 시스템 구현**
   - InGameWidget에 MissionDataTable 참조 추가 및 SetMissionTextFromIndex() 구현
   - DataTable Row 검색 로직 (FindRow<FMissionTable>) 작성
   - UI TextBlock 바인딩 및 텍스트 포맷팅 버그 수정
   - PlayerController에서 InGameWidget 생성 시 DataTable 자동 할당

3. **멀티플레이어 역할 시스템 설계 논의**
   - 하나의 GameMode에서 여러 PlayerController 사용 가능 여부 확인
   - 역할별 컨트롤러 구현 방법 3가지 비교 분석 및 가이드 제공
   - PlayerState 기반 역할 관리 방식 추천 (멀티플레이 최적화)

### 다음 작업 예정

- 블루프린트에서 MissionDataTable을 DT_People로 설정하여 테스트
- 역할별 DataTable 분기 로직 구현 (소방관/시민)
- 멀티플레이어 환경에서 역할 선택 시스템 통합
